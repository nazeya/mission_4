pipeline:
  name: simple_ci_pipeline
  identifier: simple_ci_pipeline
  projectIdentifier: Cohort43
  orgIdentifier: default
  tags: {}
  properties:
    ci:
      codebase:
        connectorRef: project_level_github_connector
        repoName: mission_4
        build: <+input>
  stages:
    - stage:
        name: build_stage
        identifier: build_stage
        description: ""
        type: CI
        spec:
          cloneCodebase: true
          caching:
            enabled: true
            override: true
          buildIntelligence:
            enabled: true
          infrastructure:
            type: KubernetesDirect
            spec:
              connectorRef: nazia_build_farm_connector
              namespace: harness-delegate-ng
              automountServiceAccountToken: true
              nodeSelector: {}
              os: Linux
          execution:
            steps:
              - step:
                  type: Run
                  name: Run Script
                  identifier: Run_Script
                  spec:
                    connectorRef: project_level_dockerhub_connector
                    image: bash
                    shell: Sh
                    command: sh run.sh
